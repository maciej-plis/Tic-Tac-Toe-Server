client.url: http://localhost:4200

spring:
  datasource:
    url: jdbc:h2:mem:tic-tac-toe-db
    driver-class-name: org.h2.Driver
  jpa:
    hibernate.ddl-auto: create
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
